// Benchmark: Fibonacci (recursive)
func fib(n) {
    if n <= 1 {
        return n
    }
    return fib(n - 1) + fib(n - 2)
}

let start = now()
let result = fib(30)
let end = now()

pln("Fibonacci(30) =", result)
pln("Time (ms):", (end - start) * 1000)
