// 复合赋值运算符测试
var a = 10
pln("初始 a = ", a)

a += 5
pln("a += 5 → ", a)

a -= 3
pln("a -= 3 → ", a)

a *= 2
pln("a *= 2 → ", a)

// 赋值为整数
a = 6
pln("a = 6 → ", a)

a %= 3
pln("a %= 3 → ", a)

// 验证最终结果
if (a == 0) {
    pln("✓ 所有复合赋值运算符测试通过")
} else {
    pln("✗ 复合赋值测试失败，预期0，实际得到", a)
}
