func factorial(n) {
    if (n <= 1) {
        return 1
    }
    return n * factorial(n - 1)
}

pln("Factorial of 10: ", factorial(10)) // Should be 3628800
pln("Factorial of 5: ", factorial(5)) // Should be 120
